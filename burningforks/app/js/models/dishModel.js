define([
    'backbone'
],function(Backbone){
    
    /*
    var Filter = Backbone.Model.extend({
      url: '/filters/',
      parse: function (response) {
        return {
          snippet: $(response)
        }
      }
    });
    */
    
    var DishModel = Backbone.Model.extend({
        
        
        
    });
    
    return DishModel;
    
});